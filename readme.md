## Toralizer

Toralizer is a lightweight C-based Linux library and command-line tool that forces other applications, such as curl or ssh, to connect through the Tor network to anonymize traffic. It acts as a SOCKS5 proxy wrapper, enabling user traffic to be routed through Tor by intercepting connect calls

